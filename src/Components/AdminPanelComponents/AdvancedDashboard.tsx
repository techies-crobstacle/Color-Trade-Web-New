import React from 'react';
import { RoundRow, GameStatsPayload } from './GameStats';

export function AdvancedDashboardPanel({ row, inModal, onOpenModal }: { row: RoundRow, inModal: boolean, onOpenModal?: () => void }) {
    // Component code will go here
}
